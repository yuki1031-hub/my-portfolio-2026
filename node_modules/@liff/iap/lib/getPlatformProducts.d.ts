export type PlatformProduct = {
    currency: string;
    price: number;
    productName: string;
};
export type GetPlatformProductsParams = {
    productIds: string[];
};
export declare function getPlatformProducts({ productIds, }: GetPlatformProductsParams): Promise<Record<string, PlatformProduct>>;
